# Managed by AwesomeArchLinux.
# Optional overlay for hosts whose network policy explicitly forbids IPv6.
# IPv6 remains enabled in every profile unless this file is selected.

net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
